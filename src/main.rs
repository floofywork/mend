// RATCHET:STUB — placeholder entrypoint so the stub crate builds and the
// frozen acceptance tests can run (and fail) before the real `mend` binary
// exists. The green phase owns the real, inert `fn main`.
fn main() {}
